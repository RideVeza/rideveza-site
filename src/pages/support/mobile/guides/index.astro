---
import HelpLayout from "../../../../layouts/HelpLayout.astro";

// Mobile app guides. Ordered: get the app & get in, then using the app.
const articles = [];
---

<HelpLayout
  title="Mobile App Guides"
  description="Help and training for the Veza Mobile App."
  back="/support/mobile"
  backLabel="‹ Back to mobile app support"
>
  <h1>Veza Support and How-tos</h1>
  <span class="eyebrow">Veza Mobile App</span>
  <p class="subtitle">
    Short guides with pictures for the Veza Mobile App. Each one shows what a
    screen does and how to use it. Pick a topic below.
  </p>

  {articles.length === 0 && <p class="subtitle">Guides are coming soon.</p>}

  <ul class="card-list">
    {articles.map((a) => (
      <li>
        <a class="topic" href={a.href}>
          <div class="topic-body">
            <h2 class="topic-title">{a.title}</h2>
            <p class="topic-blurb">{a.blurb}</p>
          </div>
          <span class="topic-arrow" aria-hidden="true">→</span>
        </a>
      </li>
    ))}
  </ul>
</HelpLayout>

<style>
  /* Card appearance comes from the shared .topic styles in /veza-ui.css. */
  .card-list {
    list-style: none;
    margin: 28px 0 0;
    padding: 0;
    display: grid;
    gap: 18px;
  }
</style>
